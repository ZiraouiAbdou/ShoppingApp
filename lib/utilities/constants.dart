import 'package:flutter/material.dart';

const blackColor = Colors.black;
const whiteColor = Colors.white;
const grey = Colors.grey;

const lineSpace = SizedBox(
  height: 20,
);

const gras = TextStyle(fontSize: 24, fontWeight: FontWeight.bold);
